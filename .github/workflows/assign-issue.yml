name: Assign Issue to Copilot

on:
  issues:
    types: [opened]

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: otto-de/assign_issue_toCopilot_action@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
